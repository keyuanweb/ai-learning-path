# 01 · 符号地图：`vllm/__init__.py` 与源码目录

**源码**：[`code/vllm/vllm/__init__.py`](../../code/vllm/vllm/__init__.py)

## 为什么先读这个文件

这是 vLLM 的「符号地图」。花 10 分钟扫完 `MODULE_ATTRS` 字典（~20 个导出符号），你就能快速定位任何类型的真实文件位置。

## `MODULE_ATTRS` 导出清单

| 导出符号 | 实际位置 | 用途 |
|----------|---------|------|
| `LLM` | `vllm.entrypoints.llm` | 离线推理主入口 |
| `LLMEngine` | `vllm.v1.engine.llm_engine` | 引擎前端（V1） |
| `AsyncLLMEngine` | `vllm.v1.engine.async_llm` | 在线服务异步引擎 |
| `EngineArgs` | `vllm.engine.arg_utils` | 引擎级配置参数 |
| `AsyncEngineArgs` | `vllm.engine.arg_utils` | 异步引擎配置 |
| `SamplingParams` | `vllm.sampling_params` | 采样参数 |
| `PoolingParams` | `vllm.pooling_params` | Pooling/embedding 参数 |
| `RequestOutput` | `vllm.outputs` | 一次请求的结果 |
| `CompletionOutput` | `vllm.outputs` | 单个 completion |
| `PoolingRequestOutput` | `vllm.outputs` | pooling 结果 |
| `EmbeddingRequestOutput` | `vllm.outputs` | embedding 结果 |
| `ClassificationRequestOutput` | `vllm.outputs` | 分类结果 |
| `ScoringRequestOutput` | `vllm.outputs` | 评分结果 |
| `TextPrompt` | `vllm.inputs` | 文本 prompt |
| `TokensPrompt` | `vllm.inputs` | token ID prompt |
| `PromptType` | `vllm.inputs` | prompt 联合类型 |
| `ModelRegistry` | `vllm.model_executor.models.registry` | 模型注册/发现 |
| `initialize_ray_cluster` | `vllm.v1.executor.ray_utils` | Ray 集群初始化 |

## 源码目录速查表

以下按架构层次列出关键目录，方便快速定位代码：

| 层次 | 目录 | 用途 |
|------|------|------|
| **入口** | `vllm/entrypoints/` | 对外 API：LLM 类、OpenAI/Anthropic 兼容服务、MCP、语音 |
| **引擎** | `vllm/v1/engine/` | V1 引擎：LLMEngine 前端、EngineCore 后端、IPC Client |
| **调度** | `vllm/v1/core/sched/` | 调度器、请求队列、调度输出 |
| **KV 缓存** | `vllm/v1/core/` | KV Cache Manager、BlockPool、Encoder Cache |
| **执行器** | `vllm/v1/executor/` | Executor 抽象：UniProc/MultiProc/Ray |
| **Worker** | `vllm/v1/worker/` | GPU/CPU/XPU Worker、GPUModelRunner(~7800行) |
| **模型层** | `vllm/model_executor/` | 模型实现(291个)、层基元、权重量化与加载 |
| **Attention** | `vllm/v1/attention/` | Attention Backend 注册与选择、MLA/FlashAttn/FlashInfer/Triton |
| **编译** | `vllm/compilation/` | torch.compile 集成、PiecewiseBackend、Fusion Passes、CUDA Graphs |
| **IR** | `vllm/ir/` | vLLM 函数式 IR：`@register_op` / `@register_impl` 模式 |
| **分布式** | `vllm/distributed/` | TP/PP/DP/EP 并行、KV Transfer、EC Transfer、EPLB |
| **KV 卸载** | `vllm/v1/kv_offload/` | 分层 KV 卸载：CPU/P2P/Filesystem + LRU/ARC 策略 |
| **投机解码** | `vllm/v1/spec_decode/` | Eagle/Medusa/N-gram proposer |
| **结构化输出** | `vllm/v1/structured_output/` | XGrammar/Outlines/LM Format Enforcer/Guidance |
| **采样** | `vllm/v1/sample/` | 采样器、logits processor、rejection sampler |
| **配置** | `vllm/config/` | Pydantic 配置系统(30+文件)：VllmConfig、ModelConfig、CompilationConfig 等 |
| **多模态** | `vllm/multimodal/` | 图像/音频/视频处理、Encoder 缓存、Processor 注册 |
| **平台** | `vllm/platforms/` | 硬件平台抽象：CUDA/ROCm/TPU/XPU/CPU |
| **插件** | `vllm/plugins/` | 可扩展架构：General/Platform/IO/Stats/Endpoint 插件 |
| **推理** | `vllm/reasoning/` | 推理输出解析器（DeepSeek R1/V3/V4, Qwen3, Gemma4…） |
| **工具** | `vllm/tool_parsers/` | 工具调用解析器（Hermes, Mistral, Llama, DeepSeek…） |
| **LoRA** | `vllm/lora/` | LoRA 适配器：层实现、Punica kernel、模型管理 |
| **Rust** | `rust/` | Rust 加速组件：Tokenizer、Parser、HTTP Server、Engine Client |
| **C 扩展** | `csrc/` | C++/CUDA kernel：Attention、MoE、Quantization、AllReduce |

## 延迟加载机制

```python
def __getattr__(name):
    if name in MODULE_ATTRS:
        module_name, attr_name = MODULE_ATTRS[name].split(":")
        module = importlib.import_module(module_name, __package__)
        return getattr(module, attr_name)
    raise AttributeError(...)
```

`__getattr__` 是 Python 的 fallback 机制：当正常属性查找失败时被调用。vLLM 用它在第一次访问符号时才 import 对应模块。这就是为什么 `import vllm` 不会立即初始化 CUDA。

## `__all__` 列表

定义了 ~20 个公开符号，包括 `__version__`, `__version_tuple__`, `LLM`, `ModelRegistry` 和各种输入/输出类型。

## 阅读时间

约 10 分钟。扫一遍即可，后续遇到不认识的类名回来查。
